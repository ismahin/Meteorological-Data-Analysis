# Optional offline SOTA evaluation dependencies. These are intentionally excluded
# from the production API image because the packaged winner uses scikit-learn only.
chronos-forecasting==2.3.0
psutil>=5.9,<8
